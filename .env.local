# Local development environment overrides
# This file is for local development only and overrides .env values

# Use local data directory instead of /data/ for local development
DATA_PATH=data/gps/

# Keep all other settings from .env file
# The --env-file flag will load .env first, then any additional ENV vars can override
