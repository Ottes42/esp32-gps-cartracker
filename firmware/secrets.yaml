# ESPHome secrets file for GPS Car Tracker
# Replace with your actual WiFi credentials for production use

# WiFi Configuration
wifi_ssid: "YourWiFiNetwork"
wifi_password: "YourWiFiPassword"

# Server Configuration (can be configured via web interface at runtime)
# These are fallback values if web configuration is not used
server_url: "http://192.168.1.100:3000"
server_auth_user: "admin" 
server_auth_pass: "password"
