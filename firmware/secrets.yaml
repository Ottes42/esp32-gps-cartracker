# ESPHome secrets file for GPS Car Tracker
# ⚠️  SECURITY WARNING: Update ALL values before production use!
# These are TEMPLATE values for development - never use in production!

# WiFi Configuration
# TODO: Replace with your actual WiFi network credentials
wifi_ssid: "YourWiFiNetwork"       # ⚠️  CHANGE THIS
wifi_password: "YourWiFiPassword"   # ⚠️  CHANGE THIS

# Server Configuration (can be configured via web interface at runtime)
# These are fallback values if web configuration is not used
server_url: "http://192.168.1.100:3000"
server_auth_user: "admin"           # ⚠️  CHANGE THIS
server_auth_pass: "password"        # ⚠️  CHANGE THIS - use strong password!
